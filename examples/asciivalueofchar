// C program to print ASCII Value of Character using
#include <stdio.h>

int main() {

    char ch = 'A';

    int asciiValue = (int)ch;

    printf("ASCII value of %c is %d\n", ch,asciiValue);
    return 0;
}